# wave 3 (2022-2024) holiday-row decisions

Generated by `tools/wave3_rows.py`; every number below is recomputed by
re-running it. The rows themselves are in `<family>.rows.rs`, the
per-family evidence tables in `<family>.evidence.md`, the row citations in
`documents.md`, and the machine-readable copy of everything here in
`SUMMARY.json`.

## 1. Inputs

| input | sha256 | notes |
|---|---|---|
| `cme-2022-2024.json` | `bf32100c6f5643d114d64286a7f0a3fed34aa15c13cd78db7047e684999a69a7` | the wave-3 evidence block |
| `raw/cme-2022-2024/INDEX.md` | `c8f200a05eddf884c93382e183411d7bf56bee2e0f1a50e0f728d49be068ea1b` | 36 indexed artifacts |
| `raw/cme-2022-2024-fix/INDEX.md` | `c85ef1188838c6967ba472881ec1ef9941b1d2d04e4ec1fbf3b1ed2adf43d751` | 11 indexed artifacts |

Audited window: **2022-01-01 .. 2024-12-31** (venue-local trade dates), declared as
`coverage: [(2022, 1, 1) ..= (2024, 12, 31)]` in every family.

The tool reads `cme-2022-2024.r2.json` when it exists and
`cme-2022-2024.json` otherwise; the file actually read is named above.
The round-2 verification verdict (`cme-2022-2024.verify.json`) records six
findings and all six are verbatim, coverage-prose or INDEX defects, so no
`status`, `close_instant`, `open_instant`, `document` or `tier` field
differs between r1 and r2 and the derived rows are identical under either.

## 2. Normal-week grid assumed per family, 2022-2024

The block is holiday evidence only. The ordinary week is the crate's own
family profile — the module the rows will live beside — read at the
revision in force for every date in this wave:

| family (group) | ordinary week | first matching open | ordinary final close | read from |
|---|---|---|---|---|
| `globex_equity_index` | one wrapped leg per trade date, 17:00 CT the previous evening into a 16:00 CT close on the trade date | `17:00 CT` (previous local date) | 16:00 CT | `cme_group.rs CME_PROFILE_DATED_CURRENT (2021-06-27, CME Globex notice 20210621)` |
| `globex_energy` | one wrapped leg per trade date, 17:00 CT the previous evening into a 16:00 CT close on the trade date | `17:00 CT` (previous local date) | 16:00 CT | `energy_metals.rs ENERGY_METALS_DATED_CURRENT (2015-09-20, CME Globex notice 20150907)` |
| `globex_fx` | one wrapped leg per trade date, 17:00 CT the previous evening into a 16:00 CT close on the trade date | `17:00 CT` (previous local date) | 16:00 CT | `fx.rs DATED_CURRENT (2010-11-15, CME Globex notice 20101025)` |
| `globex_grains` | 19:00 CT the previous evening into 07:45 CT, then the 08:30-13:20 CT day session whose close is the trading day's final closing phase, then the 14:30-16:00 CT post-close period | `19:00 CT` (previous local date) | 16:00 CT | `grains.rs DATED_CURRENT (2015-07-05, CME SER-7395R)` |
| `globex_interest_rates` | one wrapped leg per trade date, 17:00 CT the previous evening into a 16:00 CT close on the trade date | `17:00 CT` (previous local date) | 16:00 CT | `interest_rates.rs PROFILE_2011_10_02 (2011-10-02, CME Globex notice 20110926)` |
| `globex_livestock` | one flat Monday-Friday 08:30-13:05 CT session inside a single civil day, with an 08:00-08:30 CT pre-open and a 14:30-16:00 CT post-close period | `08:30 CT` (same civil day) | 16:00 CT | `livestock.rs PROFILE_CURRENT (2020-05-31, CME SER-8599R)` |
| `globex_cryptocurrency` | one wrapped leg per trade date, 17:00 CT the previous evening into a 16:00 CT close on the trade date | `17:00 CT` (previous local date) | 16:00 CT | `cryptocurrency.rs FIVE_DAY (2017-12-17, CME SER-8051R)` |
| `globex_nikkei_225_dollar` | one wrapped leg per trade date, 17:00 CT the previous evening into a 16:00 CT close on the trade date | `17:00 CT` (previous local date) | 16:00 CT | `cme_nikkei.rs NKD_CURRENT (2015-09-20, CME Globex notice 20150817)` |
| `dairy` | one wrapped leg per trade date, 17:00 CT the previous evening into a 16:00 CT close (13:55 CT on Fridays) | `17:00 CT` (previous local date) | 16:00 CT (13:55 CT on Fridays) | `block dairy rows (CME's Dairy product-group line); not a crate key` |
| `lumber` | one flat Monday-Friday 09:00-15:05 CT session inside a single civil day, with a 06:00 CT pre-open | `09:00 CT` (same civil day) | 15:05 CT | `block lumber rows (CME's Lumber product-group line); not a crate key` |

Three consequences that decide rows below:

- The wrapped families' first **matching** open is 17:00 CT (grains
  19:00 CT) on the previous local date. The 16:00-17:00 and 16:45-19:00
  pre-opens are order-entry phases, so a printed open equal to the
  ordinary matching open moves no boundary and ships no row.
- Grains' and livestock's ordinary final close in the operator's own
  printing is the `16:00 (CLOSED)` token that ends the 14:30-16:00 CT
  post-close period; their matching close is 13:20 CT and 13:05 CT. A
  printed `16:00` close is therefore the ordinary one and ships nothing,
  which is what CME's own `16:00 (CLOSED)` means on 2023-07-03,
  2024-07-03 and 2024-12-31.
- `dairy` and `lumber` are folded (section 6) and their grids above are
  their own published clocks; they are used only to read their own rows
  and are never written into a crate table.

## 3. Trade-date conversion

The crate's trade date is the venue-local date of a session's final close.

- **Wrapped families** (`globex_equity_index`, `globex_energy`,
  `globex_fx`, `globex_grains`, `globex_interest_rates`,
  `globex_cryptocurrency`, `globex_nikkei_225_dollar`): a halt or close
  printed on date D ends the session that opened on D-1, so the row lands
  on D. CME's own `TRADE DATE:` labels for these days name the following
  business day instead; the crate keys on the local date of the final
  close, so the crate's date is D and CME's label is recorded, not
  adopted. Every wrapped-family row in this wave has trade date = the
  block's own date, and no row needed a different conversion.
- **Flat family** (`globex_livestock`): the session lies inside one civil
  day, so the operator's event date is the crate's trade date.
- **A printed open that moves a boundary** is placed by the macro's own
  `LateOpen` rule: a value at or after the ordinary first open belongs to
  the previous local date, a value below it to the trade date itself.
  Every deviating open here is a day-session `08:30 CT` open on a wrapped
  family, so it lands on the local date the block prints for it.

Rows keyed to a trade date the block carries no entry for:

| family | trade date | kind | stated by |
|---|---|---|---|
| `globex_grains` | 2022-07-05 | late open 08:30 CT | `2022-independence-day-holiday-schedule.xls @2022-07-04T06:54:50Z` |
| `globex_grains` | 2023-07-05 | late open 08:30 CT | `4th-of-july-2023.pdf @2023-06-27T12:50:57Z` |
| `globex_grains` | 2023-12-26 | late open 08:30 CT | `christmas-day-2023.pdf @2026-07-19T09:52:48Z` |
| `globex_grains` | 2024-01-02 | late open 08:30 CT | `new-years-day-2024.pdf @2026-08-11T16:57:16Z` |
| `globex_grains` | 2024-07-05 | late open 08:30 CT | `CME-SVC-2024-07-03` |
| `globex_grains` | 2024-12-26 | late open 08:30 CT | `CME-SVC-2024-12-24` |

These are the day-after-closure late opens: the closure entry's printed
open is the successor trade date's own day-session open, and the trade
date is inside the wave window.

## 4. What was dropped, and why

`status: normal` ships no row. A printed instant that equals the family's
ordinary instant changes no answer and ships no row either. A printed
`08:30 CT` open ships no late open when the trade date's own printed
morning shows its prior-evening leg ran.

| drop reason | entries |
|---|---|
| open: open cell carries no clock token (early_close) | 26 |
| open: open cell carries no clock token (modified) | 1 |
| open: printed open 08:30 CT is the family's ordinary first open (early_close) | 4 |
| open: printed open 09:00 CT is the family's ordinary first open (early_close) | 4 |
| open: printed open 17:00 CT is the family's ordinary first open (early_close) | 84 |
| open: printed open 17:00 CT is the family's ordinary first open (modified) | 38 |
| open: the trade date's own printed morning shows `07:45`, so the prior-evening leg ran and the `08:30 CT` open inside it moves no boundary (early_close) | 1 |
| printed close 16:00 equals the family's ordinary 16:00 CT final close (modified) | 2 |
| printed close is stated to be the family's ordinary final close (modified) | 39 |
| reopen: open cell carries no clock token (closed) | 14 |
| reopen: printed open 08:30 CT is the family's ordinary first open (closed) | 25 |
| reopen: printed open 09:00 CT is the family's ordinary first open (closed) | 25 |
| reopen: printed open 17:00 CT is the family's ordinary first open (closed) | 62 |
| reopen: printed open 19:00 CT is the family's ordinary first open (closed) | 16 |
| status normal (normal) | 35 |
| **total** | **376** |

| group | dropped entries |
|---|---|
| `dairy` | 34 |
| `globex_cryptocurrency` | 53 |
| `globex_energy` | 56 |
| `globex_equity_index` | 34 |
| `globex_fx` | 53 |
| `globex_grains` | 23 |
| `globex_interest_rates` | 34 |
| `globex_livestock` | 34 |
| `globex_nikkei_225_dollar` | 21 |
| `lumber` | 34 |

The full per-date list is in `SUMMARY.json` under `dropped_entries`.

`modified` entries are decided from the printed instants. The 38
`globex_fx` and `globex_cryptocurrency` entries on the 19 noon-halt
holidays print a close the block itself states is the family's
ordinary final close, at a holiday halt that falls on that ordinary
instant, so they ship nothing; `globex_grains` on 2023-07-03, 2024-07-03
and 2024-12-31 prints its ordinary `16:00` post-close end and ships no
close row, and only 2024-07-03's *open* moves a boundary, which ships a
row on 2024-07-05.

## 5. Dates the operator's documents do not cover

Three holiday dates inside the window appear in the block's `missing`
list and carry no family entry at all. Silence there would read as
audited normal, so each of the eight families ships `Unsourced`:

| trade date | document | why |
|---|---|---|
| 2023-01-16 | `CME-SVC-2023-01-15` | 2023 Dr. Martin Luther King, Jr. Day (Monday 16 January 2023) |
| 2023-02-20 | `CME-SVC-2023-02-19` | 2023 Presidents Day (Monday 20 February 2023) |
| 2023-04-07 | `CME-SVC-2023-04-06` | 2023 Good Friday (Friday 7 April 2023) |

The thirteen `globex_nikkei_225_dollar` `unknown` dates are the 2024
windows the T2 service answers for ten representative products, none of
them Nikkei; they ship `Unsourced` at T2 as well. The `Unsourced` kind
clips nothing, so every one of these dates still resolves to the family's
ordinary week — which is the honest answer, not a scheduling claim.

## 6. The dairy and lumber fold

Decided for this wave: `dairy` folds into `globex_grains` and `lumber`
into `globex_livestock`, because CME publishes them under the joined
labels `Grains & Oilseeds include Dairy` and `Livestock include Lumber`.
The tool derives each folded group's own row on its own published grid
and compares it with the crate family's row for the same trade date; a
date on which both state a row and the rows differ is reported, and the
crate family's own row is the one shipped.

| group -> family | dates compared | agree | group-only | family-only | differ |
|---|---|---|---|---|---|
| `dairy` -> `globex_grains` | 36 | 26 | 0 | 6 | 4 |
| `lumber` -> `globex_livestock` | 30 | 29 | 0 | 0 | 1 |

Per-date disagreements (both sides state a row and they differ):

| group -> family | trade date | folded group says | crate family says |
|---|---|---|---|
| `dairy` -> `globex_grains` | 2022-11-25 | closed | late open 08:30 CT and early close 12:05 CT |
| `dairy` -> `globex_grains` | 2023-11-24 | closed | late open 08:30 CT and early close 12:05 CT |
| `dairy` -> `globex_grains` | 2024-11-29 | closed | late open 08:30 CT and early close 12:05 CT |
| `dairy` -> `globex_grains` | 2024-12-24 | early close 12:00 CT | early close 12:05 CT |
| `lumber` -> `globex_livestock` | 2024-12-24 | early close 12:05 CT | early close 12:15 CT |

Rows the crate family states and its folded group does not (the
day-after-closure late opens, where the folded group's own clock reopens
at its ordinary 17:00 CT evening open instead):

| group -> family | trade date | crate family says |
|---|---|---|
| `dairy` -> `globex_grains` | 2022-07-05 | late open 08:30 CT |
| `dairy` -> `globex_grains` | 2023-07-05 | late open 08:30 CT |
| `dairy` -> `globex_grains` | 2023-12-26 | late open 08:30 CT |
| `dairy` -> `globex_grains` | 2024-01-02 | late open 08:30 CT |
| `dairy` -> `globex_grains` | 2024-07-05 | late open 08:30 CT |
| `dairy` -> `globex_grains` | 2024-12-26 | late open 08:30 CT |

Beyond the per-date rows, the two clocks themselves differ and no row
comparison can show it: dairy's ordinary session is the wrapped
17:00-16:00 CT leg while `globex_grains` is 19:00 -> 07:45 plus
08:30-13:20 CT; lumber's ordinary session is 09:00-15:05 CT while
`globex_livestock` is 08:30-13:05 CT. Both are raised as questions.

## 7. Document-id assignment

- **T1** ids are `<file-name> @<capture UTC>`, taken from the INDEX's
  `file` and `capture (UTC)` columns: e.g.
  `2022-mlk-day-holiday-schedule.xls @2022-01-17T21:22:30Z`. The capture
  timestamp is the Wayback capture, never the retrieval date.
- **T2** ids are `CME-SVC-<first fromEventDate in the queried window>`,
  read from the `fromEventDate=` token of the INDEX's `source url`
  column: e.g. `CME-SVC-2024-01-14` for `api_2024-01-14.json`.
- The operator's finalised 2022 Thanksgiving vintage is saved under a
  distinct local name (`2022-thanksgiving-holiday-schedule.FINAL-20221122.xls`)
  and its id uses that name, so the preliminary capture of the same URL
  cannot be confused with it. The preliminary vintage keys no row.
- Every id used by a row appears in `tools/out/documents.md` and nowhere
  else: 30 ids, 17 T1 and 13 T2.
- Artifacts read but keying no row — the 2022 New Year's workbook (all
  ten of its entries are `normal`), `api_2022-12-31.json`, the clearing
  advisories and every CDX index response — are not in `documents.md`,
  because that table is the resolution table for row citations only.

## 8. Verification performed

- Every family's rows are strictly ascending by trade date and inside
  2022-01-01..2024-12-31.
- Every row's document id resolves to an INDEX row whose sha256 and byte
  count were recomputed from the saved file.
- Every `instant as printed` token is a substring of the block's own text
  for the entry the row cites.
- Re-running the tool on the same inputs is byte-identical;
  `tools/check_wave3.py` re-runs it, re-parses the emitted `.rows.rs`
  files independently and recomputes these checks from the outputs.
- In-tool check results: **PASS**.

## 9. Questions to rule on

**Q1. `globex_nikkei_225_dollar` on 2022-05-30: the printed close `01:00 CT` falls in the small hours of the trade date, before the ordinary day session begins at `08:30 CT`.**

The block records status `early_close` at that instant, and the tool ships `early_close(1 * 3_600)`. On the 2022-05-30 sheet the outright Nikkei and BTIC Nikkei lines are merged under one label and the printed instant is the BTIC-style `1:00 (close)`; every other 2022 sheet prints a separate `Nikkei` row governed by the EQUITIES `12:00` halt, and the block's own verbatim calls the merge 'a residual ambiguity'. Ruling needed: ship `01:00`, or the EQUITIES `12:00`, or `Unsourced`?

Tool default: ship early_close(1 * 3_600) as the block states.

**Q2. The `dairy` -> `globex_grains` fold joins two different clocks.**

`dairy`'s ordinary week is one wrapped leg per trade date, 17:00 CT the previous evening into a 16:00 CT close (13:55 CT on Fridays); `globex_grains`'s is 19:00 CT the previous evening into 07:45 CT, then the 08:30-13:20 CT day session whose close is the trading day's final closing phase, then the 14:30-16:00 CT post-close period. A family table holds one scalar boundary per trade date, so a folded group whose own session moves at a different instant cannot be represented by the crate family's row; the tool compares the two row by row on each group's own grid and ships the crate family's row.

Tool default: keep the fold for status only; the crate family's instants stand.

**Q3. The `lumber` -> `globex_livestock` fold joins two different clocks.**

`lumber`'s ordinary week is one flat Monday-Friday 09:00-15:05 CT session inside a single civil day, with a 06:00 CT pre-open; `globex_livestock`'s is one flat Monday-Friday 08:30-13:05 CT session inside a single civil day, with an 08:00-08:30 CT pre-open and a 14:30-16:00 CT post-close period. A family table holds one scalar boundary per trade date, so a folded group whose own session moves at a different instant cannot be represented by the crate family's row; the tool compares the two row by row on each group's own grid and ships the crate family's row.

Tool default: keep the fold for status only; the crate family's instants stand.

**Q4. Fold disagreement on 2022-11-25: `dairy` says closed, `globex_grains` says late open 08:30 CT and early close 12:05 CT.**

Folding `dairy` into `globex_grains` gives one table; on this trade date the two lines state different answers, and the tool ships the crate family's row without changing it. Ruling needed: does the folded group close or shorten the crate family's trade date here?

Tool default: ship the crate family's row and record the disagreement.

**Q5. Fold disagreement on 2023-11-24: `dairy` says closed, `globex_grains` says late open 08:30 CT and early close 12:05 CT.**

Folding `dairy` into `globex_grains` gives one table; on this trade date the two lines state different answers, and the tool ships the crate family's row without changing it. Ruling needed: does the folded group close or shorten the crate family's trade date here?

Tool default: ship the crate family's row and record the disagreement.

**Q6. Fold disagreement on 2024-11-29: `dairy` says closed, `globex_grains` says late open 08:30 CT and early close 12:05 CT.**

Folding `dairy` into `globex_grains` gives one table; on this trade date the two lines state different answers, and the tool ships the crate family's row without changing it. Ruling needed: does the folded group close or shorten the crate family's trade date here?

Tool default: ship the crate family's row and record the disagreement.

**Q7. Fold disagreement on 2024-12-24: `dairy` says early close 12:00 CT, `globex_grains` says early close 12:05 CT.**

Folding `dairy` into `globex_grains` gives one table; on this trade date the two lines state different answers, and the tool ships the crate family's row without changing it. Ruling needed: does the folded group close or shorten the crate family's trade date here?

Tool default: ship the crate family's row and record the disagreement.

**Q8. Fold disagreement on 2024-12-24: `lumber` says early close 12:05 CT, `globex_livestock` says early close 12:15 CT.**

Folding `lumber` into `globex_livestock` gives one table; on this trade date the two lines state different answers, and the tool ships the crate family's row without changing it. Ruling needed: does the folded group close or shorten the crate family's trade date here?

Tool default: ship the crate family's row and record the disagreement.

**Q9. The day-after-closure late opens on 2022-07-05, 2023-07-05, 2023-12-26, 2024-01-02, 2024-07-05, 2024-12-26 have no counterpart in the folded group.**

On these dates the crate family's own document states that the prior-evening leg did not run and the day session opened at 08:30 CT, so the tool ships a `LateOpen`; the folded group's own clock reopens at its ordinary evening open and states no row, so it treats the same trade date as ordinary. This is the fold's clock difference showing up as a row and not just prose.

Tool default: ship the crate family's `LateOpen` rows.

**Q10. 6 rows are keyed to trade dates the block has no entry for.**

Every one is a `LateOpen` the crate family's own document states for the day after a closure (globex_grains 2022-07-05 (late open 08:30 CT); globex_grains 2023-07-05 (late open 08:30 CT); globex_grains 2023-12-26 (late open 08:30 CT); globex_grains 2024-01-02 (late open 08:30 CT); globex_grains 2024-07-05 (late open 08:30 CT); globex_grains 2024-12-26 (late open 08:30 CT)). The block carries one entry per holiday date, so the trade date that follows a closure has no entry of its own and the tool creates the row from the closure entry's printed open.

Tool default: keep the created rows.

**Q11. 14 closure entries print no reopen at all.**

The 2024 Good Friday windows are the cause: the T2 service was read for 2024-03-28 .. 2024-03-30, so the Sunday 2024-03-31 reopen (and with it whether the trade date 2024-04-01 lost its prior-evening leg, which matters for `globex_grains`) is outside the document. The tool ships no row for 2024-04-01. Per group: dairy: 2023-11-23, 2023-11-24, 2024-03-29, 2024-11-28, 2024-11-29; globex_cryptocurrency: 2024-03-29; globex_energy: 2024-03-29, 2024-03-29; globex_equity_index: 2024-03-29; globex_fx: 2024-03-29; globex_grains: 2024-03-29; globex_interest_rates: 2024-03-29; globex_livestock: 2024-03-29; lumber: 2024-03-29.

Tool default: ship nothing for 2024-04-01 and record the gap.

**Q12. The 3 uncovered 2023 dates cite the operator's own negative control as their document id.**

`holidays!` fails the build for a row with an empty document id, so an `Unsourced` row must name an artifact. The tool cites the saved T2 response that returns `hasEvents:false` for that window (CME-SVC-2023-01-15, CME-SVC-2023-02-19, CME-SVC-2023-04-06), which is the operator's own machine channel read as bytes. The alternative is to split the family's coverage into three windows and claim no window over those dates. Two consequences are deliberately left unsourced: the day *after* each of these three holidays (2023-01-17, 2023-02-21 and, for `globex_grains`, 2023-04-10) may have lost its prior-evening leg the way 2024-01-02 and 2023-12-26 did, and no document in the store states whether it did, so those trade dates ship no row and the crate reports the family's ordinary week there.

Tool default: cite the negative control, keep one 2022-2024 window.

**Q13. `globex_nikkei_225_dollar` ships 16 `Unsourced` rows, 13 of them the whole 2024 window.**

The T2 service returns only the ten representative products and no Nikkei is among them, so the 2024 Nikkei dates are inside the family's window but carry no operator statement. The rows clip nothing and keep the window contiguous. Ruling needed: confirm, or shrink this family's coverage to 2022-01-01..2023-12-31 and let 2024 be 'no answer'?

Tool default: keep the contiguous window with `Unsourced` rows.

**Q14. 3 document ids take their tier from the INDEX's channel statement, not from a per-row tier token.**

The fix INDEX's section 2 rows (`CME-SVC-2023-01-15, CME-SVC-2023-02-19, CME-SVC-2023-04-06`) carry no `T1`/`T2` token in their description column; the file states the channel is `tier T2 under LAW-PRIMARY-SOURCES` on line 14. The tool reads that as the file-level default.

Tool default: accept the file-level tier statement.

**Q15. The wave's dates 2022-01-01 carry `normal` for all ten product groups, so no family ships any row for them.**

2022-01-01 is a Saturday, so no family has a trade date there; the 2022 New Year's workbook prints the ordinary Thursday/Friday/Sunday grid around it. Ruling needed: confirm that this holiday date correctly ships nothing, rather than being an earlier round's gap.

Tool default: ship nothing.

